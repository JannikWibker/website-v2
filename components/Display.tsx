import { styled } from '../stitches.config'

const Display = styled('div', {
  maxWidth: '80vw',
  minWidth: '20vw',
  width: '512px',
  textAlign: 'left',
  margin: 'auto'
})

export default Display
